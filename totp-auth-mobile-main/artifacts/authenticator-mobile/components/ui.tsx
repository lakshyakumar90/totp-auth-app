import React from 'react'; import { ActivityIndicator, Pressable, StyleSheet, Text, TextInput, View } from 'react-native'; import { Feather } from '@expo/vector-icons'; import { useColors } from '@/hooks/useColors';
export function IconButton({ icon, onPress, label }: { icon: keyof typeof Feather.glyphMap; onPress: () => void; label: string }) { const c = useColors(); return <Pressable accessibilityLabel={label} testID={label} onPress={onPress} style={({ pressed }) => [styles.iconButton, pressed && { opacity: .6 }]}><Feather name={icon} size={20} color={c.foreground} /></Pressable>; }
export function PrimaryButton({ title, onPress, loading = false, disabled = false }: { title: string; onPress: () => void; loading?: boolean; disabled?: boolean }) { const c = useColors(); return <Pressable testID={title} disabled={disabled || loading} onPress={onPress} style={({ pressed }) => [styles.primary, { backgroundColor: c.primary }, (pressed || disabled) && { opacity: .7 }]}>{loading ? <ActivityIndicator color={c.primaryForeground} /> : <Text style={[styles.primaryText, { color: c.primaryForeground }]}>{title}</Text>}</Pressable>; }
export function Field({ label, value, onChangeText, placeholder, secureTextEntry = false, autoCapitalize = 'none' as const }: { label: string; value: string; onChangeText: (v: string) => void; placeholder: string; secureTextEntry?: boolean; autoCapitalize?: 'none' | 'sentences' | 'words' | 'characters' }) { const c = useColors(); return <View style={styles.field}><Text style={[styles.label, { color: c.mutedForeground }]}>{label}</Text><TextInput value={value} onChangeText={onChangeText} placeholder={placeholder} placeholderTextColor={c.mutedForeground} secureTextEntry={secureTextEntry} autoCapitalize={autoCapitalize} autoCorrect={false} style={[styles.input, { color: c.foreground, backgroundColor: c.input, borderColor: c.border }]} /></View>; }
export const styles = StyleSheet.create({ iconButton: { width: 42, height: 42, alignItems: 'center', justifyContent: 'center' }, primary: { minHeight: 52, borderRadius: 14, alignItems: 'center', justifyContent: 'center', paddingHorizontal: 20 }, primaryText: { fontFamily: 'Inter_700Bold', fontSize: 15 }, field: { gap: 8 }, label: { fontFamily: 'Inter_600SemiBold', fontSize: 12, letterSpacing: .8, textTransform: 'uppercase' }, input: { minHeight: 52, borderRadius: 12, borderWidth: 1, paddingHorizontal: 16, fontFamily: 'Inter_500Medium', fontSize: 16 } });
